# define bad referers via regex (1 = forbidden, 0 = allowed)
map $http_referer $bad_referer {
    hostnames;

    default                           0;

    # Put regexes for undesired referers here
    "~buy-cheap-online.info"          1;
}
