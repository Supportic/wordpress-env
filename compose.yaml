volumes:
  db_data:
    name: ${PROJECT_NAME}-dbdata
networks:
  default:
    name: ${PROJECT_NAME}-network
    driver: bridge

services:
  ########################
  # Wordpress
  ########################
  wordpress:
    container_name: ${PROJECT_NAME}-wordpress
    image: ${PROJECT_NAME}/wordpress:0.0.1
    hostname: ${PROJECT_NAME}-wordpress
    user: ${UID:-1000}
    restart: unless-stopped
    build:
      context: ./.docker/service/wordpress
      args:
        - UID=${UID}
        - GID=${GID}
        - UNAME=${UNAME}
        - GNAME=${GNAME}
        - PASS=${PASS}
        - WP_VERSION=${WP_VERSION}
        - PHP_VERSION=${PHP_VERSION}
      target: develop
    init: true
    working_dir: /var/www/html
    volumes:
      - ./wordpress:/var/www/html
      - ./wp-dev:/home/${UNAME}/wp-dev
    env_file:
      - .wordpress.env
    environment:
      WORDPRESS_DB_HOST: ${PROJECT_NAME}-db:3306
      WORDPRESS_DB_NAME: ${DB_NAME}
      WORDPRESS_DB_USER: ${DB_USER}
      WORDPRESS_DB_PASSWORD: ${DB_PASS}
      # for symlink.sh, absolute paths only
      DEV_DIR: /home/$UNAME/wp-dev
      WORDPRESS_DIR: /var/www/html
    depends_on:
      db:
        condition: service_healthy
      memcached:
        condition: service_started
    # make curl localhost possible inside this container (throws error when container shuts down quickly)
    # post_start:
    #   - command: bash -c "echo \"--resolve localhost:80:$(php -r 'echo gethostbyname(\"nginx\");')\" > /home/${UNAME}/.curlrc"
    #     user: ${UID:-1000}

  ########################
  # Webserver
  ########################
  nginx:
    container_name: ${PROJECT_NAME}-nginx
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "${WORDPRESS_PORT_HOST:-80}:80"
    volumes:
      - ./wordpress:/var/www/html
      - ./.docker/data/logs/nginx:/var/log/nginx
      - ./.docker/service/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./.docker/service/nginx/conf.d:/etc/nginx/conf.d:ro
    healthcheck:
      test: curl --fail http://localhost:${WORDPRESS_PORT_HOST:-80} || exit 1
      start_period: 10s
      interval: 60s
      timeout: 5s
      retries: 3
    depends_on:
      wordpress:
        condition: service_started
    post_start:
      - command: chown -R ${UID}:${GID} /var/log/nginx
        user: root

  wpcli:
    container_name: ${PROJECT_NAME}-wpcli
    image: ${PROJECT_NAME}/wpcli:0.0.1
    user: ${UID:-1000}
    build:
      context: ./.docker/service/wpcli
      args:
        - UID=${UID}
        - GID=${GID}
        - UNAME=${UNAME}
        - GNAME=${GNAME}
        - PASS=${PASS}
        - PHP_VERSION=${PHP_VERSION}
    init: true
    # make curl localhost possible inside this container
    network_mode: "service:nginx"
    env_file:
      - .wordpress.env
    environment:
      WORDPRESS_DB_HOST: ${PROJECT_NAME}-db
      WORDPRESS_DB_NAME: ${DB_NAME}
      WORDPRESS_DB_USER: ${DB_USER}
      WORDPRESS_DB_PASSWORD: ${DB_PASS}
      # for setup-wordpress.sh
      WORDPRESS_PORT_HOST: ${WORDPRESS_PORT_HOST:-80}
    # mirrors filesystem of the listed services
    volumes_from:
      - wordpress
    depends_on:
      db:
        condition: service_healthy

  ########################
  # Database
  ########################
  db:
    container_name: ${PROJECT_NAME}-db
    image: mariadb:11.4
    user: mysql
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "healthcheck.sh", "--connect", "--innodb_initialized" ]
      start_period: 10s
      interval: 10s
      timeout: 5s
      retries: 3
    environment:
      MARIADB_DATABASE: ${DB_NAME}
      MARIADB_USER: ${DB_USER}
      MARIADB_PASSWORD: ${DB_PASS}
      MARIADB_ROOT_PASSWORD: ${DB_ROOTPASS}
    volumes:
      - db_data:/var/lib/mysql
      - ./.docker/service/mysql/my.cnf:/etc/mysql/conf.d/my.cnf:ro

  memcached:
    image: memcached:alpine
    container_name: ${PROJECT_NAME}-memcached
    restart: unless-stopped
    expose:
      - 11211
    command:
      - --conn-limit=1024
      - --memory-limit=64
      - --threads=4

  ########################
  # Database UI
  ########################
  adminer:
    container_name: ${PROJECT_NAME}-adminer
    image: adminer:latest
    user: adminer
    restart: unless-stopped
    environment:
      # https://github.com/vrana/adminer/tree/master/plugins
      # listing plugins here will move them from /plugins to /plugins-enabled
      ADMINER_PLUGINS: "tables-filter edit-calendar edit-textarea"
      # https://github.com/vrana/adminer/tree/master/designs
      # ADMINER_DESIGN: "nicu"
      # default server entry in the login form
      # ADMINER_DEFAULT_SERVER: "db"
      ADMINER_SERVERS_JSON: '[{"server":"db:3306","username":"${DB_USER}","password":"${DB_PASS}","db":"${DB_NAME}","label":"Local Dev WordPress"}]'
    ports:
      - "${ADMINER_PORT_HOST:-8080}:8080" # web ui
    volumes:
      - ./.docker/service/adminer/plugins/autologin.php:/var/www/html/plugins/autologin.php
      - ./.docker/service/adminer/plugins-enabled/autologin.php:/var/www/html/plugins-enabled/autologin.php

  ########################
  # Email Catcher
  #######################
  mailpit:
    container_name: ${PROJECT_NAME}-mailpit
    image: axllent/mailpit:latest
    restart: unless-stopped
    expose:
      - 1025 # smtp server
    ports:
      - "${MAILPIT_PORT_HOST:-8025}:8025" # web ui
    environment:
      TZ: Europe/Berlin
